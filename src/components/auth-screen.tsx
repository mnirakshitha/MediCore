"use client";

import Link from "next/link";
import { ArrowLeft, CheckCircle2, Eye, EyeOff, FlaskConical, LockKeyhole, Mail, ShieldCheck } from "lucide-react";
import { useRouter } from "next/navigation";
import { useState, type FormEvent } from "react";
import { demoAccounts } from "@/lib/medicore-data";
import { useSession } from "@/components/session-context";

type Mode = "login" | "register" | "forgot" | "reset";
export function AuthScreen({ mode }: { mode: Mode }) { const router = useRouter(); const { setUser } = useSession(); const [showPassword, setShowPassword] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const [notice, setNotice] = useState(""); const [email, setEmail] = useState(mode === "login" ? "admin@medicore.com" : ""); const isLogin = mode === "login"; const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setError(""); const form = new FormData(event.currentTarget); const password = String(form.get("password") || ""); if (!email.includes("@")) { setError("Please enter a valid email address."); return; } if (mode === "forgot") { setNotice("If a MediCore account exists for this email, a secure reset link has been sent."); return; } if (mode === "reset") { if (password.length < 8) { setError("Create a password with at least 8 characters."); return; } setNotice("Your password has been reset. You can now sign in."); return; } if (mode === "register") { if (password.length < 8) { setError("Create a password with at least 8 characters."); return; } setNotice("Your request has been received. A hospital administrator will activate your secure account."); return; } setLoading(true); try { const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email, password }) }); const result = await response.json() as { token?: string; user?: { name: string; email: string; role: "ADMIN" | "DOCTOR" | "RECEPTIONIST" | "PHARMACIST" | "ACCOUNTANT" | "PATIENT" }; message?: string }; if (!response.ok || !result.user || !result.token) { setError(result.message || "Unable to sign in. Please try again."); return; } window.localStorage.setItem("medicore-token", result.token); setUser(result.user); router.push("/dashboard"); } catch { setError("Network error. Please check your connection and try again."); } finally { setLoading(false); } };
  const heading = mode === "login" ? "Welcome back" : mode === "register" ? "Create your access request" : mode === "forgot" ? "Reset your password" : "Choose a new password"; const subtitle = mode === "login" ? "Sign in to your secure MediCore workspace." : mode === "register" ? "Start your hospital workspace onboarding securely." : mode === "forgot" ? "Enter your email and we’ll send a secure reset link." : "Create a strong new password for your account."; return <main className="grid min-h-screen bg-[#f7fafa] lg:grid-cols-[.94fr_1.06fr]"><section className="relative hidden overflow-hidden bg-slate-900 p-10 text-white lg:flex lg:flex-col"><div className="absolute inset-0 bg-[radial-gradient(circle_at_15%_20%,rgba(20,184,166,.35),transparent_34%),radial-gradient(circle_at_85%_85%,rgba(37,99,235,.22),transparent_34%)]" /><Link href="/" className="relative flex items-center gap-2.5"><span className="grid size-9 place-items-center rounded-xl bg-teal-500 text-white"><FlaskConical size={20} /></span><span><b className="block text-base">MediCore</b><small className="text-[9px] font-bold uppercase tracking-[.17em] text-teal-300">Hospital OS</small></span></Link><div className="relative my-auto max-w-md"><div className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[.12em] text-teal-200"><ShieldCheck size={13} />Secure healthcare operations</div><h1 className="mt-6 text-4xl font-extrabold leading-tight tracking-tight">Care delivery is better when every workflow connects.</h1><p className="mt-5 leading-7 text-slate-300">A calm, role-aware workspace for patient care, clinical coordination, pharmacy, billing, and hospital operations.</p><div className="mt-9 grid grid-cols-2 gap-3">{[["Role-aware", "Access controls"], ["Connected", "Care workflows"], ["Insightful", "Live reporting"], ["Protected", "Clinical data"]].map(([one, two]) => <div key={one} className="rounded-xl border border-white/10 bg-white/5 p-3"><p className="font-bold text-teal-200">{one}</p><p className="mt-1 text-xs text-slate-400">{two}</p></div>)}</div></div><p className="relative text-xs text-slate-500">© 2026 MediCore Health Systems</p></section><section className="flex items-center justify-center p-5 sm:p-10"><div className="w-full max-w-[430px]"><Link href="/" className="mb-10 flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-teal-700"><ArrowLeft size={16} />Back to MediCore</Link><div className="lg:hidden"><span className="grid size-9 place-items-center rounded-xl bg-teal-600 text-white"><FlaskConical size={20} /></span></div><h2 className="mt-5 text-3xl font-extrabold tracking-tight text-slate-900">{heading}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{subtitle}</p>{notice && <div className="mt-6 flex gap-2 rounded-xl border border-emerald-100 bg-emerald-50 p-3 text-sm leading-5 text-emerald-700"><CheckCircle2 size={18} className="mt-0.5 shrink-0" />{notice}</div>}{error && <div className="mt-6 rounded-xl border border-rose-100 bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}<form onSubmit={submit} className="mt-7 space-y-4">{mode === "register" && <label className="form-label">Full name<input required className="input mt-1.5" placeholder="Your full name" /></label>}<label className="form-label">Email address<div className="relative mt-1.5"><Mail size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="input pl-9" placeholder="name@hospital.com" /></div></label>{mode !== "forgot" && <label className="form-label">{mode === "reset" ? "New password" : "Password"}<div className="relative mt-1.5"><LockKeyhole size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input required name="password" type={showPassword ? "text" : "password"} defaultValue={isLogin ? "Demo@123" : ""} className="input pl-9 pr-10" placeholder={mode === "reset" ? "At least 8 characters" : "Enter your password"} /><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-600" aria-label="Show password">{showPassword ? <EyeOff size={16} /> : <Eye size={16} />}</button></div></label>}{isLogin && <div className="flex items-center justify-between"><label className="flex items-center gap-2 text-xs font-semibold text-slate-500"><input type="checkbox" defaultChecked className="size-4 rounded border-slate-300 accent-teal-600" />Remember me</label><Link href="/forgot-password" className="text-xs font-bold text-teal-600 hover:text-teal-700">Forgot password?</Link></div>}<button disabled={loading} type="submit" className="button-primary mt-2 w-full justify-center py-3 text-sm disabled:cursor-not-allowed disabled:opacity-60">{loading ? "Signing in securely..." : mode === "login" ? "Sign in" : mode === "register" ? "Submit access request" : mode === "forgot" ? "Send reset link" : "Reset password"}</button></form>{isLogin && <div className="mt-7"><div className="flex items-center gap-3"><span className="h-px flex-1 bg-slate-200" /><span className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Demo accounts</span><span className="h-px flex-1 bg-slate-200" /></div><div className="mt-3 grid gap-2 sm:grid-cols-2">{demoAccounts.map((account) => <button key={account.email} onClick={() => setEmail(account.email)} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-left transition hover:border-teal-300 hover:bg-teal-50"><span className="block text-[10px] font-extrabold text-teal-700">{account.role}</span><span className="mt-0.5 block truncate text-[11px] text-slate-500">{account.email}</span></button>)}</div><p className="mt-3 text-center text-[11px] text-slate-400">All demo accounts use <b>Demo@123</b></p></div>}<p className="mt-7 text-center text-sm text-slate-500">{mode === "login" ? <>New to MediCore? <Link href="/register" className="font-bold text-teal-600">Request access</Link></> : <>Already have access? <Link href="/login" className="font-bold text-teal-600">Sign in</Link></>}</p></div></section></main>; }
