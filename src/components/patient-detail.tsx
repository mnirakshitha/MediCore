"use client";

import Link from "next/link";
import { useState } from "react";
import { ArrowLeft, CalendarDays, CreditCard, Edit3, FileText, Phone, ShieldCheck, UserRound } from "lucide-react";
import { appointments, bills, patients, prescriptions } from "@/lib/medicore-data";
import { AppShell } from "@/components/app-shell";
import { StatusBadge } from "@/components/shared-ui";

export function PatientDetail({ id }: { id: string }) { const patient = patients.find((item) => item.id === id) ?? patients[0]; const [tab, setTab] = useState("Overview"); const appointmentRows = appointments.filter((item) => item.patient === patient.name).slice(0, 4); const prescriptionRows = prescriptions.filter((item) => item.patient === patient.name); const billRows = bills.filter((item) => item.patient === patient.name); const tabs = ["Overview", "Appointments", "Medical records", "Prescriptions", "Bills"]; return <AppShell><div><Link href="/patients" className="mb-5 inline-flex items-center gap-1.5 text-xs font-bold text-slate-500 hover:text-teal-700"><ArrowLeft size={15} />Back to patients</Link><section className="surface overflow-hidden"><div className="border-b border-slate-100 p-5 sm:p-6 dark:border-slate-800"><div className="flex flex-col gap-5 sm:flex-row sm:items-start sm:justify-between"><div className="flex gap-4"><span className="grid size-14 place-items-center rounded-2xl bg-teal-100 text-lg font-extrabold text-teal-700 dark:bg-teal-500/20 dark:text-teal-300">{patient.name.split(" ").map((part) => part[0]).join("")}</span><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-xl font-extrabold text-slate-900 dark:text-white">{patient.name}</h1><StatusBadge value={patient.status} /></div><p className="mt-1 font-mono text-xs font-bold text-slate-400">{patient.id}</p><div className="mt-3 flex flex-wrap gap-x-4 gap-y-1 text-xs text-slate-500"><span className="flex items-center gap-1"><Phone size={13} />{patient.phone}</span><span>{patient.email}</span></div></div></div><button className="button-secondary"><Edit3 size={15} />Edit profile</button></div><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4"><Mini label="Age / gender" value={`${patient.age} · ${patient.gender}`} /><Mini label="Blood group" value={patient.blood} /><Mini label="Insurance" value={patient.insurance} /><Mini label="Registered" value={patient.registered} /></div></div><div className="flex overflow-x-auto px-4 sm:px-6">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`shrink-0 border-b-2 px-3 py-3 text-xs font-bold transition ${tab === item ? "border-teal-600 text-teal-700 dark:text-teal-300" : "border-transparent text-slate-500 hover:text-slate-700 dark:hover:text-slate-300"}`}>{item}</button>)}</div></section><section className="mt-5">{tab === "Overview" && <Overview patient={patient} />}{tab === "Appointments" && <AppointmentTab rows={appointmentRows} />}{tab === "Medical records" && <RecordsTab />}{tab === "Prescriptions" && <PrescriptionTab rows={prescriptionRows} />}{tab === "Bills" && <BillsTab rows={billRows} />}</section></div></AppShell>; }
function Mini({ label, value }: { label: string; value: string }) { return <div className="rounded-lg bg-slate-50 p-3 dark:bg-slate-800"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 truncate text-xs font-bold text-slate-700 dark:text-slate-200">{value}</p></div>; }
function Overview({ patient }: { patient: typeof patients[number] }) { return <div className="grid gap-5 xl:grid-cols-[1.1fr_.9fr]"><div className="surface p-5"><h2 className="text-sm font-extrabold text-slate-800 dark:text-white">Patient overview</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><div><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Contact information</p><dl className="mt-3 space-y-3 text-xs"><div><dt className="text-slate-400">Phone</dt><dd className="mt-0.5 font-bold text-slate-700 dark:text-slate-200">{patient.phone}</dd></div><div><dt className="text-slate-400">Email</dt><dd className="mt-0.5 font-bold text-slate-700 dark:text-slate-200">{patient.email}</dd></div><div><dt className="text-slate-400">Emergency contact</dt><dd className="mt-0.5 font-bold text-slate-700 dark:text-slate-200">Daniel Bennett · +1 202 555 2281</dd></div></dl></div><div><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Care & coverage</p><dl className="mt-3 space-y-3 text-xs"><div><dt className="text-slate-400">Blood group</dt><dd className="mt-0.5 font-bold text-slate-700 dark:text-slate-200">{patient.blood}</dd></div><div><dt className="text-slate-400">Known allergies</dt><dd className="mt-0.5 font-bold text-slate-700 dark:text-slate-200">Penicillin</dd></div><div><dt className="text-slate-400">Insurance plan</dt><dd className="mt-0.5 font-bold text-slate-700 dark:text-slate-200">{patient.insurance}</dd></div></dl></div></div></div><div className="surface p-5"><h2 className="text-sm font-extrabold text-slate-800 dark:text-white">Care timeline</h2><div className="mt-5 space-y-5 border-l border-slate-200 pl-5 dark:border-slate-700">{[["Today", "Upcoming cardiology review", "Dr. Amelia Roberts · 09:00 AM"], ["Mar 24", "Medical record updated", "Hypertension follow-up documented"], ["Mar 24", "Invoice payment received", "INV-26091 · $620.00"], ["Mar 14", "Prescription issued", "Losartan 50mg · 30 days"]].map(([date, title, detail]) => <div key={title} className="relative"><span className="absolute -left-[25px] top-1 size-2.5 rounded-full bg-teal-500 ring-4 ring-teal-50 dark:ring-teal-500/10" /><p className="text-[10px] font-bold text-teal-600">{date}</p><p className="mt-1 text-xs font-bold text-slate-700 dark:text-slate-200">{title}</p><p className="mt-1 text-[11px] text-slate-500">{detail}</p></div>)}</div></div></div>; }
function AppointmentTab({ rows }: { rows: typeof appointments }) { return <TabCard icon={<CalendarDays size={18} />} title="Appointment history">{rows.length ? rows.map((row) => <div key={row.id} className="flex items-center justify-between border-b border-slate-100 py-4 last:border-0 dark:border-slate-800"><div><p className="text-xs font-bold text-slate-700 dark:text-slate-200">{row.department} · {row.reason}</p><p className="mt-1 text-[11px] text-slate-400">{row.doctor} · {row.date} at {row.time}</p></div><StatusBadge value={row.status} /></div>) : <p className="py-8 text-center text-sm text-slate-500">No appointment history available.</p>}</TabCard>; }
function RecordsTab() { return <TabCard icon={<FileText size={18} />} title="Medical record history"><div className="rounded-xl border border-slate-100 p-4 dark:border-slate-800"><div className="flex justify-between gap-4"><div><p className="text-xs font-bold text-slate-800 dark:text-white">Hypertension follow-up</p><p className="mt-1 text-[11px] text-slate-400">Dr. Amelia Roberts · Mar 24, 2026</p></div><span className="rounded bg-teal-50 px-2 py-1 text-[10px] font-bold text-teal-700 dark:bg-teal-500/15 dark:text-teal-300">Read-only</span></div><div className="mt-4 grid gap-4 text-xs sm:grid-cols-3"><div><p className="text-slate-400">Vitals</p><p className="mt-1 font-bold text-slate-700 dark:text-slate-200">BP 128/82 · 98.4°F</p></div><div><p className="text-slate-400">Diagnosis</p><p className="mt-1 font-bold text-slate-700 dark:text-slate-200">Essential hypertension</p></div><div><p className="text-slate-400">Follow-up</p><p className="mt-1 font-bold text-slate-700 dark:text-slate-200">Apr 21, 2026</p></div></div></div></TabCard>; }
function PrescriptionTab({ rows }: { rows: typeof prescriptions }) { return <TabCard icon={<ShieldCheck size={18} />} title="Active prescriptions">{rows.map((row) => <div key={row.id} className="rounded-xl border border-slate-100 p-4 dark:border-slate-800"><div className="flex justify-between"><div><p className="text-xs font-bold text-slate-800 dark:text-white">{row.medicine}</p><p className="mt-1 text-[11px] text-slate-400">{row.id} · {row.date}</p></div><StatusBadge value="Active" /></div><p className="mt-3 text-xs text-slate-500">Diagnosis: {row.diagnosis}</p></div>)}</TabCard>; }
function BillsTab({ rows }: { rows: typeof bills }) { return <TabCard icon={<CreditCard size={18} />} title="Billing history">{rows.map((row) => <div key={row.id} className="flex items-center justify-between border-b border-slate-100 py-4 last:border-0 dark:border-slate-800"><div><p className="text-xs font-bold text-slate-700 dark:text-slate-200">{row.id} · {row.department}</p><p className="mt-1 text-[11px] text-slate-400">{row.date}</p></div><div className="text-right"><p className="text-sm font-extrabold text-slate-800 dark:text-white">${row.total}.00</p><StatusBadge value={row.paymentStatus} /></div></div>)}</TabCard>; }
function TabCard({ icon, title, children }: { icon: React.ReactNode; title: string; children: React.ReactNode }) { return <div className="surface p-5"><div className="mb-4 flex items-center gap-2 text-teal-600"><span>{icon}</span><h2 className="text-sm font-extrabold text-slate-800 dark:text-white">{title}</h2></div><div className="space-y-3">{children}</div></div>; }
